# Neural Chromium Website - Environment Variables

# GitHub Personal Access Token for Build Ledger
# Required to avoid GitHub API rate limiting (403 errors)
# Create a token at: https://github.com/settings/tokens
# Permissions needed: public_repo (read-only)
NEXT_PUBLIC_GITHUB_TOKEN=your_github_token_here
